package com.almundo.app;

import com.almundo.app.CallCenterEmployee;

class Supervisor extends CallCenterEmployee {
    public Supervisor(Dispatcher dispatcher, int capacity) {
        super(dispatcher, capacity);
    }
}
