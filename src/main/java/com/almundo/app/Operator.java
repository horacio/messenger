package com.almundo.app;

import com.almundo.app.CallCenterEmployee;

class Operator extends CallCenterEmployee {
    public Operator(Dispatcher dispatcher, int capacity) {
        super(dispatcher, capacity);
    }
}
