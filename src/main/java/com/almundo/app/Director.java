package com.almundo.app;

import com.almundo.app.CallCenterEmployee;

class Director extends CallCenterEmployee {
    public Director(Dispatcher dispatcher, int capacity) {
        super(dispatcher, capacity);
    }
}
